# Momentus Event Lifecycle Map — Claude Context

## What This Project Is
The **Momentus Event Lifecycle Map** is a strategic alignment tool for PM, Engineering, and Design — a shared vision of the human value Momentus is meant to deliver. It is:

- **Value-driven and human/user-centric** — grounded in personas, jobs-to-be-done, and desired outcomes
- **Product-agnostic and holistic** — maps the full event management lifecycle, not a single product
- **Strategic** — surfaces Momentus' current value delivery coverage, gaps (integration gaps, third-party tool usage, pain points), and opportunities

Its purpose is to align teams around: where we are today, where we want to go, and how we get there together.

## Primary Artifact
- **`index.html`** — the interactive map, a single-file plain HTML/CSS/JS application
- Hosted on GitHub Pages so changes are immediately shareable via URL
- This file is the source of truth — all design and content work happens here

## Companion Docs (generated from `index.html`, for LLM/coworker consumption)
- **`SITE_CONTENT.md`** — a full plain-text mirror of every piece of content on the site (personas, objects, North Star metrics, full JTBD map). Auto-generated — never hand-edit. Upload this to a Claude Project so any coworker can ask deep content questions without needing to open or screenshot the live site.
- **`ARCHITECTURE.md`** — documents the content *model*: the schema behind personas/objects/JTBD/North Star, ID conventions, and exactly where new validated content belongs. Read this before adding content or guiding a coworker's contribution.
- **Regenerate after any content change:** `cd tools && node extract-content.js` rewrites `SITE_CONTENT.md` from the current `index.html`. Re-upload it to the Claude Project afterward. The `tools/` folder (a small Node script + jsdom) is a maintenance utility only — it's not part of the shipped site.

## Personas
Four core user personas plus the cross-cutting End-to-End track, each with an assigned color used consistently across the map. Canonical order everywhere they appear as a sequence: Sales → Planning → Operations → Financial → End-to-End — the "main 3" (Sales/Planning/Operations) stay first and together by design. As of 2026-07-16, no persona has a personal name anywhere on the map — every persona is identified by role/job title only, both in the live content and in conversation about this project (internal ids below are code-only, never used as display names).
- **`sharon`** — Sales Manager, Sales — `#34d399` (green)
- **`emilie`** — Event Planner, Planning — `#60a5fa` (blue)
- **`owen`** — Operations Director, Operations — `#f97316` (orange)
- **`dana`** — Finance Director, Financial — `#eab308` (yellow) — added 2026-07-06, see Session Log
- **`me`** — Venue Director, End-to-End / North Star metric — `#a78bfa` (purple) — reserved exclusively for this; renamed from `e3` on 2026-07-10 to match the ME North Star acronym, see Session Log

## Design System (defined in `:root` CSS variables)
- **Fonts:** IBM Plex Sans (body), IBM Plex Mono (data/code), Sora (display/headings)
- **Background:** `#0d0f14` (dark)
- **Surface layers:** `#13161e`, `#1a1e2a`
- **Attribute type colors:** Fixed Conditions (purple), Dynamic States (sky blue), Structural Status (orange)
- **North Star input metric colors:** Adoption (green), Engagement (sky), Retention (orange), Efficiency (fuchsia), Effectiveness (purple)
- All color tokens are defined as CSS custom properties — always use variables, never hardcode hex values

## Project Stage & Workflow
- **Living document** — continuously refined as stakeholders contribute expertise and feedback
- Andrea conducts regular **validation interviews** with stakeholders; Claude helps synthesize insights and apply feedback to the map
- No Jira or Confluence for this project — all context and inputs are provided directly in conversation

## Supporting Files (reference only, do not modify)
- `Event Lifecycle & Verticals.pptx` — source presentation material
- `PMO-Booking Manager_ KPI & North Star Metric*.pdf`, `PMO-Event Coordinator_ KPI & North Star Metric*.pdf` — North Star metric definitions per persona
- `Amplitude-The-North-Star-Playbook.pdf`, `North-Star_how-to-Guide_2024.pdf` — methodology references
- `UX_Magic_SemanticIxD_Summary.doc` — semantic IxD methodology reference
- `jobs_to_be_done_book.pdf`, `jtbd canvas.webp` — JTBD methodology references
- `EAB Executive Advisory Board & Executive Track/` — executive input materials

## How to Work on This Project
- **Editing the map:** Modify `index.html` directly. Keep all styles in the `<style>` block and all logic in the `<script>` block — single-file constraint must be maintained for GitHub Pages compatibility
- **After any content edit:** regenerate `SITE_CONTENT.md` (`cd tools && node extract-content.js`) and re-upload it to the Claude Project used for coworker Q&A. See `ARCHITECTURE.md` for the content schema and where new content belongs.
- **Synthesizing interviews:** When Andrea shares interview notes, extract themes, map them to existing lifecycle phases/personas/jobs, and surface actionable changes to the map
- **Design decisions:** Respect the established color system and design tokens. Propose changes clearly before implementing
- **Confirm before making significant structural changes** to the map's content model (phases, personas, jobs, metrics)
- **No bare acronyms** (added 2026-07-10, see Session Log) — always show the full name. Industry-standard acronyms (CRM, ERP, GL, ROI, CFO, NPS, GDPR, AI, F&B, AV, BEO, JTBD, ODI, IxD, etc.) may get a "(ACRONYM)" pairing; Momentus-invented/internal acronyms (MO, MP, MW, MF, ME, EAB, OPD) never do — just the full name, always. Rule of thumb: expand the *first* bare mention within a given card/modal/JTBD-stage view, then later mentions in that same view may stay bare. Compact UI elements (attribute chips, field labels, tags with tight width) are exempt and keep the short form. Never alter acronyms inside verbatim research quotes — for custom/uncertain terms inside a quote, add the expansion inline instead, e.g. `MWO (Mobile Work Orders)`.

## Session Log

### 2026-07-01 — Built the content-export system
**Problem:** Andrea tried loading `index.html` into a Claude Project so coworkers could ask it questions about the map, but Claude asked for screenshots — the site's real content lives inside ~20 JS data objects in the `<script>` block, not as visible static text, so the raw file wasn't queryable as-is. She also wants Claude to deeply understand the map's structure so it can add new validated content to the right place and guide coworkers through contributing.

**What was built:**
- `SITE_CONTENT.md` — full plain-text export of every persona (+ vertical variants + secondary personas), all 4 primary objects, all 4 North Star metrics, and the entire JTBD map (4 personas × 8 stages × 300+ desired-outcome statements). This is what should be uploaded to the Claude Project for coworker Q&A.
- `ARCHITECTURE.md` — the content model/schema: ID conventions, the ODI outcome grammar, and exactly where each type of new content belongs. Read this before adding content or guiding a contribution.
- `tools/extract-content.js` (+ `tools/package.json`, needs `npm install` once) — regenerates `SITE_CONTENT.md` from `index.html` on demand, so the export stays in sync as Andrea keeps editing the map. Uses Node's `vm` to read the JS data objects and `jsdom` to parse the static HTML (North Star cards, Semantic Layer object cards). Run via `cd tools && node extract-content.js`.

**Decisions made:** Chose auto-regeneration over a one-time manual snapshot, since Andrea edits `index.html` often and a static doc would drift.

**Discovered along the way (not yet acted on):**
- The hidden `.zi-grid` persona cards in `#sec-personas` (`display:none`) contain stale/superseded quotes that don't match the live persona modals — candidate for cleanup, not touched.
- `OPPS_ACTIONS` data still exists in the code but isn't rendered anywhere in the current UI (the feature that displayed it was removed at some point).

**State at end of session:** Nothing committed to git — `SITE_CONTENT.md`, `ARCHITECTURE.md`, and `tools/` are new untracked files, same as everything else in this working directory. `SITE_CONTENT.md` has not yet been uploaded to the Claude Project — that's the next step for Andrea to do manually.

### 2026-07-06 — Added the Historic Context section, then a full 4th product area ("Financial")

**Part 1 — Historic Context (Strategy Layer)**
Built a new section below North Stars: 4 cards (mirroring the North Star card/modal shell) that open a vertical, filterable timeline of Momentus/Ungerboeck company history, categorized Industry/Market · Customer · Product (Andrea's own draft categorization, kept as-is).
- Sources: `gomomentus.com/ungerboeck` (the only source with real pre-2023 dates — founding 1985 through the 2023 Momentus rebrand and WeTrack acquisition), the 2023 Event Technology Awards blog post, and all pre-existing local docs (PMO strategy PDFs, EAB/Illuminate docs, `Event Lifecycle & Verticals.pptx`). The blog's other ~98 posts are evergreen SEO content, not history — not mined.
- Data model: a new `HISTORIC_CONTEXT` JS array (`{sort, dateLabel, category, area, title, desc, source, sourceUrl}` per entry) plus `openHcModal()`/`hcFilter()` — chosen over the North Star pattern's hidden-DOM cloning because this list will keep growing. Each entry has a source citation (real link for the 2 public-web-sourced facts; plain-text filename citation for internal docs, since those aren't hosted anywhere public).
- The Sales/Planning/Operations cards show ONLY entries tagged to that specific functional area (not the universal/company-wide ones) — Andrea asked for this explicitly to reduce noise since she plans to keep feeding in more area-specific source material. The End-to-End card shows everything. Sorted newest-first.
- Card icons: each area's real Semantic-Layer object icon (contract/clipboard/checklist/calendar) with a small clock badge in the corner — same treatment as the North Star cards' star badge, swapped for a clock to signal "history."
- **Gap:** Sales has 6 area-specific entries, Operations 8, Planning only 2 — genuinely thin, not padded. More sources from Andrea will fill these in.

**Part 2 — Added "Financial" as a full 4th product area**
Andrea provided 6 new files (`financials_persona.md/.docx/.pptx`, `financials_ai_opportunities.docx`, `FinanicalsSurveyQuestions.docx`, `FinanicalsSurveyResults.docx`) and asked for a full persona/object/JTBD/North Star build, positioned **Sales → Planning → Operations → Financial → End-to-End** (the "main 3" stay first/together — this ordering is now the documented convention, see `ARCHITECTURE.md`).
- **Source quality note:** all 3 underlying interviews (Laura Knott/Raleigh CC, Gary Firlit/Rochester Floreano CC, Stacey Church/Georgia World Congress Center) are Convention Center customers — single-vertical, n=3, qualitative. Same "not every persona has every vertical populated" state the original 3 personas started in. No `VERTICAL_ATTRS` or `SECONDARY_PERSONAS` entries exist yet for `dana` — that's an honest gap, not an oversight.
- **Identity (confirmed with Andrea before implementing):** internal id `dana`, display name **Casey Lindgren**, title **Finance Director**, archetype **"The Financial Steward"** (verbatim from the source doc), object **"Event Financials"** (Andrea's refinement of my draft), North Star **"Momentus Financials (MF)"** — defined as full financial closeout completed entirely inside Momentus without falling back to PeopleSoft/spreadsheets. Color yellow (`--dana: #eab308` dark / `#a16207` light).
- **Important, deliberate decision:** MF is a **peer metric** alongside MO/MP/MW — I did **not** edit the existing ME (End-to-End North Star) definition or rationale text to require financial closeout. If Andrea later wants ME redefined to include Financial, that's a separate, bigger content change — flag it explicitly, don't assume.
- **JTBD:** full 8-stage map built (44 outcome statements total, unevenly distributed — e.g. Locate/Confirm are thin at 4 each, Conclude is richest at 8 — because that's what the actual research supports; not padded to match the other personas' uniform 8-per-stage).
- Implementation touched all 4 layers plus ~20 mechanical ordering locations (North Star/Human Layer/Stages-View cards, `PERSONAS_J`/`SHORT`/`MAP` array order — which drives the JTBD grid column order directly — icon lookup tables, gradient connectors, `tools/extract-content.js`'s own hardcoded persona/object order). Swept the file systematically for every hardcoded 4-item lookup keyed by persona/object id rather than assuming the plan's initial location list was exhaustive — it wasn't; found several more (`objMeta` in `openObjectModal`, `PERSONA_ICONS` in `openPersonaModal`, `pIcons` in the JTBD grid builder) during a `grep`-driven sweep after the "planned" locations were done.
- **Icon iterations** (Andrea gave feedback across several follow-up turns): Event Financials object icon went dollar-sign → receipt-with-$ → coin (circle+$), settling on the coin to match the persona-icon motif already used elsewhere for Financial. The North Star card's icon had the coin and the star badge overlapping/illegible — fixed by dropping the $ from that specific icon and keeping just the star centered in the coin.
- **Process note for future sessions:** a general-purpose Explore agent asked only to report the *existing* code schema went off-brief and fabricated an entirely different, ungrounded persona (name "Taylor Morgan," object "Event Financial Record," North Star "Momentus Profitability," purple color). Caught by re-reading the real source docs and the real `index.html` myself before writing anything — don't trust sub-agent output for creative/content decisions without verification, even when the ask was "just report the schema."

**State at end of session:** Both features fully implemented in `index.html`, JS syntax-verified, `SITE_CONTENT.md` regenerated (and `tools/extract-content.js` updated to know about `dana`/`financials` going forward). Still to do: re-upload the regenerated `SITE_CONTENT.md` to the Claude Project; a live browser click-through (the in-session preview tooling had port-config issues this session, so verification was static/code-level, not visual — worth an actual look before treating this as fully done); decide whether Financial should get a Historic Context card too (not requested, not built). Nothing committed to git.

### 2026-07-06 (cont'd) — Added the Financial ("Dana") card to Historic Context

Answered the previous session's open question yes — built the 4th Historic Context card, following the exact pattern of the Sales/Planning/Operations cards.

- **3 new `HISTORIC_CONTEXT` entries, `area:'financial'`:** Apr 2024 Enterprise Accounting adoption/NPS-by-module snapshot (from `PMO-Enterprise Accounting & Financials - Product strategy.pdf`, same doc already used for 2 existing `universal` entries); Mar 2026 EAB session theme *"We do not know how much we're worth"* / True Event Profitability (from `illuminate_2026_internal_synthesis_1.docx` — this doc had only been mined for Sales-AI/GDPR content before; the profitability theme was sitting in it unused); Jul 2026 AI-Powered Opportunities strategy for the Financial persona (from `financials_ai_opportunities.docx`, a source not previously used anywhere on the site). All read directly from source docs myself, not delegated to a sub-agent, per the process note above.
- **New card:** `hcpb-dana`, positioned between Operations and End-to-End (Sales → Planning → Operations → Financial → End-to-End order), added `dana` to `HC_CARD_META`. Icon reuses Dana's coin motif; the coin's `$` path was then removed because it visually collided with the clock badge overlay — same fix already applied to the North Star card's coin+star icon last session. Kept only the plain coin ring + clock badge.
- **Bug found and fixed, unrelated to Financial specifically:** `tools/extract-content.js` never had `HISTORIC_CONTEXT`/`HC_CARD_META` in its vm-extraction `names` list, so the *entire* Historic Context feature (including the original Sales/Planning/Operations cards from earlier this session) was silently absent from `SITE_CONTENT.md` since it was built. Added both names to the extraction list and a new "Historic Context (Strategy Layer)" render section (grouped by card, same area-filter logic as `renderHcModal()` in the page itself, universal entries only surface on the End-to-End card). Regenerated and spot-checked the output.
- **Preview tooling:** the OneDrive root `.claude/launch.json` only had an unrelated `mwo-home` config (a different local project on port 5555). Added an `event-lifecycle-map` entry (serves this folder via `npx serve` on port 4173) alongside it, rather than replacing it. Verified live in-browser this time — card position/styling, modal open, category filter chips, and the End-to-End card picking up the new entries all confirmed working via screenshots.

**State at end of session:** `index.html` and `tools/extract-content.js` changes are in place, `SITE_CONTENT.md` regenerated to include Historic Context for the first time. Not yet uploaded to the Claude Project. Nothing committed to git. **Andrea's explicit next-session ask:** verify *all* existing site content against the available local source docs (not just Financial), and add "no data yet" empty states — modeled on the Historic Context pattern — wherever content on the site isn't actually source-backed. This is a big cross-cutting content-audit task, not yet started.

### 2026-07-07 — Ran the content audit; fixed one real bug, added one empty state

Ran the cross-cutting audit Andrea asked for at the end of the previous session. Converted all `.docx`/`.pptx`/`.rtf` sources to plain text (`textutil` + a small pptx-slide-XML extraction script, scratch folder deleted after), then ran 5 parallel sub-agents (one per persona area) to cross-reference every layer (persona/verticals/secondary personas, Semantic object, North Star, Historic Context, JTBD) against the source docs, each reporting a Strong/Partial/Thin/None confidence rating with citations.

**Process note:** two sub-agent findings turned out to be wrong and were caught by re-checking primary sources directly myself, consistent with the standing rule from the 2026-07-06 session — don't trust sub-agent output on content/factual judgments without verification, even for "just verify this" tasks:
- One agent claimed "Owen Olsen doesn't exist in the Stadiums & Arenas source material at all." False — he has his own real Private Events–track content (own quotes, own pain points) in `Momentus Personas by Vertical.docx`; the agent had only found the separate Public Events track (Bennett Bailey / Paige Pepper's world) and missed Owen's actual section.
- Another agent claimed the Financial Historic Context card's customer/NPS figures (746 total, 45%/27%/28%, NPS −16/−4/+4) were unverifiable, sitting in un-extractable chart images. False — opened `PMO-Enterprise Accounting & Financials - Product strategy.pdf` directly and confirmed every figure verbatim in plain-text tables (pages 5–6). The *other* agent auditing that same card had actually gotten this one right.

**Real bug found and fixed:** the site's Stadiums & Arenas variant of Owen Olsen (`VERTICAL_ATTRS.owen['Stadiums & Arenas']`) had misattributed content — it used Bennett Bailey/Paige Pepper's Public Events (game-day) pain points (static game-manifest PDF, change tracking, mobile gaps) under Owen's name, and overstated his scope as covering "both private events and public events." Rewrote fixed/dynamic/structural to use Owen's own sourced Private Events content (manual work orders, translating event needs into work orders by hand, end-of-day actuals close-out) and corrected the scope claim to make clear Public Events/game-day is a separate role Owen doesn't own. Also fixed Hank Henry's (`SECONDARY_PERSONAS.owen`) structural-status line, which incorrectly named "operations (Owen)" as one of the three functions risk spans — changed to a generic reference to the separate building-ops/game-day role since Bennett Bailey isn't modeled as an actual secondary persona on the site (deliberately didn't invent a bracketed name-reference to a non-existent card).

**Empty state added:** `openPersonaModal()` (`index.html` ~line 3540) rendered a "Secondary Personas" section header with a blank row underneath for Dana, since `SECONDARY_PERSONAS.dana` is genuinely empty (no verticals/secondary personas exist yet for Financial — a known, already-documented gap). Added a conditional dashed-border placeholder ("No secondary personas documented yet — not yet covered by validation interviews.") so the section reads as an intentional gap rather than a broken/missing render. Verified live in-browser (screenshot) alongside a regression check that Sharon's real secondary-persona cards still render normally.

**Other audit findings surfaced to Andrea, not yet acted on (her call):**
- All 5 primary personas' "quote" fields (Alex/Sam/Jordan/Casey/Morgan) are presented as direct speech but don't exist verbatim in any source — they're composites of the vertical variants (expected/by-design), but the quote framing implies literal speech. Flagged for Andrea to decide: relabel as illustrative, or swap in a real verbatim quote from a variant.
- MO North Star's name/definition diverges from the one directly-relevant KPI source (`PMO-Booking Manager_ KPI & North Star Metric.pdf`, which proposes "Net Booking Velocity" instead) — could be an intentional strategic reframe, needs Andrea's confirmation either way.
- Historic Context's ~9 pre-2023 entries citing `gomomentus.com/ungerboeck` aren't locally re-verifiable — confirmed `article.html` in the project root is NOT a saved copy of that page (it's an unrelated snapshot titled "Momentus Business & Product Strategy"). Not necessarily wrong, just can't be re-checked without a live fetch.
- JTBD desired-outcome statements across all 5 personas: stage *themes* are well-grounded, but individual ODI-grammar sentences are synthesized rather than transcribed from research — expected given the methodology, just worth knowing the actual granularity of "sourced" here.
- Everything else audited came back Strong or Partial — vertical variants and secondary personas (except the Owen bug above) verbatim-match `Momentus Personas by Vertical.docx`; the Financial area is the best-sourced of all five end to end.

**State at end of session:** Both fixes live in `index.html`, verified in-browser. `SITE_CONTENT.md` regenerated to reflect the Owen Olsen text change. Not yet uploaded to the Claude Project. Nothing committed to git. Remaining audit findings (quote framing, MO metric name, unverifiable pre-2023 history citations) are surfaced above but intentionally left for Andrea to decide on, not auto-fixed.

### 2026-07-07 (cont'd) — Acted on the 3 remaining audit findings

Andrea decided on all three open items from earlier in the session:

1. **Relabeled the persona quotes.** All 5 primary personas' `quote` fields are composite/synthesized, not verbatim — added a small caption directly under the quote in `openPersonaModal()` (`index.html` ~line 3566): "Composite voice — synthesized across validated interviews, not a direct quote." One shared render function serves all 5 personas, so this single change covers Alex/Sam/Jordan/Casey/Morgan at once. Left the existing `quoteSrc` data field (`'Alex Smith'` etc.) untouched — it's already unused/dead in rendering, out of scope for this fix.
2. **Folded velocity into MO's definition, kept the MO name.** The one directly-relevant KPI source (`PMO-Booking Manager_ KPI & North Star Metric.pdf`) proposed a different metric name ("Net Booking Velocity") entirely — Andrea's call was to keep "Momentus Opportunities (MO)" as the name/framework but fold the velocity concept (rate of qualified-inquiry-to-signed-contract conversion) into what MO actually measures, rather than adopt the source's name or add a separate metric. Edited the `.star-def` text for `#ns-sharon` (`index.html` ~line 1592) to combine the existing "entirely inside Momentus" framing with "the velocity of that conversion: the elapsed time from qualified inquiry to signed contract." Left the 5 key-input-metric cards and rationale Q&As untouched — didn't expand scope beyond the definition itself.
3. **Filled the exact gap the audit flagged:** fetched `https://gomomentus.com/ungerboeck` live (WebFetch) and saved its full timeline content as a new local reference doc, `gomomentus-ungerboeck-history.docx` — this URL is cited by ~9 Historic Context entries but had no local copy to verify against (confirmed `article.html` in the project root is unrelated). Generated via `docx` (docx-js), verified by round-tripping through `textutil -convert txt` since the skill's Python `validate.py` failed locally on a `match` statement (Python 3.9 vs. the 3.10+ syntax it needs — an environment issue, not a doc-generation issue). This is a supporting/reference file per the "Supporting Files" convention in this doc — not wired into `extract-content.js`, not meant to be cited from JS, just a locally-verifiable copy of what Historic Context already cites by URL.

**State at end of session:** All 3 changes live in `index.html`, `SITE_CONTENT.md` regenerated again, `gomomentus-ungerboeck-history.docx` added to the project root. Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-07 (cont'd) — Moved Secondary Personas out of the persona dialog onto the page

Andrea asked (referring to it as "the Semantic Layer," but the actual target — confirmed by inspecting the code — is the Human Layer's `#sec-personas` section, since Secondary Personas are Human Layer content; the Semantic Layer only holds the 4 primary objects) to stop hiding Secondary Personas inside the primary persona's dialog and instead show them, styled consistently with the primary persona cards, below each area's primary card.

- **Layout:** the single 5-wide `.product-row` of primary cards is now 5 `.persona-col` columns (Sales/Planning/Operations/Financial/End-to-End). Each column keeps the existing primary `.product-block` on top and adds a `.sec-persona-list` below it, populated by the new `buildSecondaryPersonaTiles()` (called once at page load, alongside `buildJTBDGrid()`). Dana's (Financial) column has no secondary personas yet, so it shows the existing dashed-border "not yet covered by validation interviews" placeholder inline instead of only inside a dialog.
- **Interaction (Andrea's explicit choice):** each `.sec-persona-tile` opens its own dialog (`openSecondaryPersonaModal()`) reusing the same `persona-modal` shell/overlay as the primary persona dialog, just labeled "`<Area>` Secondary Persona" instead of "Primary Persona," showing the secondary persona's `relationship` prose (in place of a quote) and a `vertical` tag badge, then the same Fixed/Dynamic/Structural attribute grid.
- `openPersonaModal()` no longer renders a "Secondary Personas" section at all — `toggleSecCard()` (the old in-dialog expand/collapse) was removed and replaced by the new modal function. `_attrGrid()` and `PERSONA_ICONS` were hoisted out of `openPersonaModal()` to module scope so both persona modal functions can share them.
- Removed the now-fully-dead `.sec-card`/`.sec-card-role`/`.sec-card-name`/`.sec-card-arch`/`.sec-card-rel` CSS (only ever used by the old in-dialog secondary cards). Kept `.sec-card-row`/`.sec-attrs-wrap` — those are still used by the unrelated, unwired Secondary Objects placeholder in the Semantic Layer (see "Semantic Layer — Primary Objects" in `ARCHITECTURE.md`).
- No underlying content changed (`SECONDARY_PERSONAS` data itself is untouched), so `SITE_CONTENT.md` does not need regenerating — `tools/extract-content.js` reads the JS data directly, not the modal markup.
- Verified live in-browser (screenshots): 5-column layout renders correctly in dark and light themes, primary dialog no longer shows secondary personas, a secondary tile (Owen/Operations → Paige Pepper) opens its own correctly-labeled dialog with relationship text + vertical tag + attributes, Dana's empty-state placeholder shows inline. No console errors.

**State at end of session:** Change is live in `index.html`, `ARCHITECTURE.md` updated to describe the new render location. Nothing committed to git.

**Follow-up same session — tile content reformatted:** Andrea asked the on-page `.sec-persona-tile` content to show `"Secondary Persona"` (static label) / role / vertical instead of role/name/archetype. Updated `buildSecondaryPersonaTiles()` so `.sec-persona-tile-role` now holds the literal string `"Secondary Persona"`, `.sec-persona-tile-name` holds `sp.role`, and `.sec-persona-tile-arch` holds `sp.vertical` — reused the existing three CSS classes/visual hierarchy, just remapped which field fills each slot. `sp.name` and `sp.archetype` are no longer shown on the tile (still visible after clicking through to `openSecondaryPersonaModal()`, which is unchanged). Verified live in-browser, no console errors.

### 2026-07-10 — Sitewide acronym cleanup + `e3` → `me` rename

Andrea's complaint: the map uses too many acronyms bare, which cuts against its stated purpose as a cross-team onboarding/alignment tool. New standing rule added to "How to Work on This Project" above.

- **Caught and fixed first, unprompted:** the End-to-End track's internal id was `e3` (a nickname) while its North Star metric is `ME` (Momentus Events) — mismatched. Confirmed scope with Andrea, then did a full mechanical rename `e3` → `me` across every CSS class (`.zo-e3` → `.zo-me`, `.gt-e3-*`, `.sc-e3`, `.ph-e3*`, etc.), DOM id (`nspb-e3`, `pb-persona-e3`, `hcpb-e3`, `ns-e3`, `sec-tiles-e3`, `sec-stage-e3`, `sv-e3`), JS object key (`e3:{...}` in `colorMap`/`nsMeta`/`STAGE_GRADS`/`PERSONAS_J`/`OPPS_ACTIONS`/etc.), and function-call argument (`openPersonaModal('e3')`, `openNsModal('e3')`, `openHcModal('e3')`, `openJobTimelineModal('e3')`) — used a word-boundary-safe `perl -pi -e 's/\be3\b/me/g'` after confirming via grep that no hex color (`#190e38`, `#0d1e3c`) would collide. Updated `tools/extract-content.js`'s hardcoded persona-order arrays and `ARCHITECTURE.md`'s ID docs to match. The 4 remaining visible-text/aria-label/comment mentions of literal "E3" were reworded to "End-to-End" by hand. Verified live (screenshots): End-to-End persona modal, North Star tab, and Historic Context card all resolve correctly under the new id, no console errors.
- **The actual ask — audited every acronym in visible text** (not CSS/JS-internal names, which are out of scope) via a full Explore-agent sweep, then verified/extended it myself by hand — the agent's initial pass missed a large cluster of `MWO`/`BEO` mentions buried in Owen's JTBD `productDocOpps` text, consistent with the project's standing "don't trust sub-agent output on content without verification" rule.
- **Rule negotiated with Andrea in stages, scope grew each time:** (1) start with just the 5 custom North Star metrics (MO/MP/MW/MF/ME) → she said do it for every acronym sitewide; (2) compact UI chips/field-labels (e.g. "BEO Number") keep the short form, everything else spells out; (3) never edit acronyms inside verbatim research quotes, but for the 4 flagged custom/uncertain terms in quotes (MWO, PACE, CES, OSB) add the expansion inline, e.g. `MWO (Mobile Work Orders)`; (4) BEO alone appears ~80× concentrated in Emilie's JTBD outcome grid — agreed to expand only the *first* bare mention within a single view (one JTBD stage's `step`+`researchOpps`+`productDocOpps`+`outcomes`, one persona/vertical card, one North Star card) and leave the rest of that same view bare; (5) same per-view rule extended to every other dense industry acronym (AV, F&B, GL, CRM, AI), not just BEO.
- **PACE is not an acronym** — confirmed with Andrea it's a named hospitality/CVB report type (booking-pace-vs-prior-year), not an abbreviation for anything; left untouched everywhere. Fetched `elitesupportcenter.ungerboeck.com`'s Revenue Pace Report article at Andrea's request and used it to enrich Sharon's Convention Centers vertical attribute text (`index.html` ~line 2921) with an actual definition of what a PACE report measures.
- **Custom/non-industry acronyms get zero parenthetical, ever** — MO/MP/MW/MF/ME (except the one intentional "Full Name (ACRONYM)" header per North Star card), OPD, and EAB (Executive Advisory Board — Momentus' own customer advisory program, confirmed non-industry-standard) were fully replaced with their spelled-out names throughout, including inside the Strategy Layer's `stratDesc` citation and the JTBD/ODI methodology footer citations.
- **New acronyms found during the sweep that weren't in the original ask, fixed the same way:** OEO (Online Event Ordering, confirmed via `Momentus Personas by Vertical.docx`), CES (Conference & Event Services), OSB (Online Space Booking), BI, ML, NLP, SIS, CAGR, EMS.
- **Left untouched, confirmed dead code:** `OPPS_ACTIONS` (the `action:` strings around index.html ~5150-5187) is unrendered — flagged as dead in the 2026-07-01 session log entry and reconfirmed here via `getOppAction()`'s call sites — so its bare `MWO`/`AI`/`P&L` mentions were not edited.
- Regenerated `SITE_CONTENT.md`. Verified live in-browser after every major batch (North Star cards, the Planning "Prepare Event Plan" JTBD stage) — no console errors, first-mention expansions read naturally and don't bloat the terse ODI outcome-statement grammar.

**State at end of session:** All changes live in `index.html`, `ARCHITECTURE.md`, `tools/extract-content.js`, and this file. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-14 (cont'd) — Acronym hover-underline + popover feature

Andrea asked to visually mark every acronym instance: a purple underline, hover/focus popover with the full name + a short description, hidden on mouse-leave/blur.

- **`ACRONYM_GLOSSARY`** (`index.html` ~line 2699, top of the main `<script>` block) — one entry per acronym found in the previous session's sweep (`MO`/`MP`/`MW`/`MF`/`ME`, `CRM`, `BEO`, `GL`, `ERP`, `AI`, `ROI`, `CFO`, `NPS`, `GDPR`, `AV`/`A/V`, `CAGR`, `EMS`, `SIS`, `KPI`, `OSB`, `CES`, `MWO`, `OEO`, `P&L`, `BI`, `ML`, `NLP`, `F&B`, `JTBD`, `ODI`, `IxD`, `ARR`), each `{full, desc}`. Wrote the one-sentence descriptions myself from context already gathered in the prior session — not sourced from anywhere new.
- **Found two more bare `ARR` mentions** the original acronym sweep missed (Historic Context entries at ~3845/3847, "WeTrack ARR snapshot" and "~$22M ARR") — same root cause as the earlier MWO/BEO miss: the sub-agent audit undercounts, always verify with a fresh grep before considering a sweep done. Fixed both.
- **`enhanceAcronyms(root)`** (~line 2761) — a `document.createTreeWalker`-based function that finds glossary-term text nodes inside `root` (skipping `SCRIPT`/`STYLE`/`TEXTAREA` and anything already inside a `.acr-term` span, so it's idempotent/safe to call more than once on the same content) and wraps each match in `<span class="acr-term" data-acr="KEY" tabindex="0">`, wired to `showAcrPopover`/`hideAcrPopover` on `mouseenter`/`mouseleave`/`focus`/`blur`.
- **Wiring — chose a `MutationObserver` over manually hooking every render function.** There are 25+ `.innerHTML =` call sites across the file (every modal, the JTBD grid, secondary-persona tiles, etc.); hand-editing each one is exactly the kind of enumeration that produced the missed-MWO/missed-ARR bugs above. Instead: one `enhanceAcronyms(document.body)` call plus one `MutationObserver` on `document.body` (`childList`/`subtree`) at the end of the `INIT` section (~line 5544) that re-runs `enhanceAcronyms()` on any newly-added element — covers every current and future dynamic-content path with one piece of code, no enumeration to maintain.
- **CSS:** new `--acr-line` token (`#c084fc` dark / `#9333ea` light) — deliberately distinct from `--me`'s purple so the acronym-hover affordance doesn't read as an End-to-End/ME semantic color. `.acr-term` is a plain 2px `border-bottom`; `#acr-popover` reuses the exact positioning/clamping logic already used by the existing click-to-open `#layer-info-popover` (viewport-edge clamping on both axes), just retriggered on hover instead of click.
- **Bug caught before it shipped:** `tools/extract-content.js` runs the inline script in a Node `vm` sandbox (to read the JS data objects without a real browser) with a permissive fake `document` — but no `MutationObserver` global at all, so `node extract-content.js` crashed with `ReferenceError: MutationObserver is not defined`. Fixed with a `typeof MutationObserver !== 'undefined'` guard around the observer setup; `enhanceAcronyms(document.body)` itself is already safe in the sandbox since the fake `document.body` is `undefined` and the function no-ops on a falsy root. Regenerated `SITE_CONTENT.md` successfully after the fix.
- Verified live in-browser: North Star card (`MO`/`CRM`), Semantic Layer object modal's compact `.attr-name` chips (`BEO Number`, `F&B`, `AV` — confirmed these compact labels get the same treatment, not just prose), and the Planning "Prepare Event Plan" JTBD stage (dense `BEO`/`F&B`/`AV` outcome list). Confirmed: underline renders in both themes, popover shows correct full name + description on hover, disappears on mouse-leave, no console errors, light theme legible.

**Follow-up same session — fixed missing popover on Semantic/Strategy layer info cards, dropped `AI`:**
- Andrea reported the hover popover wasn't appearing on the Semantic/Strategy layer cards. Root cause: the `MutationObserver` callback only called `enhanceAcronyms()` on `mut.addedNodes` with `nodeType === 1` (Elements) — but `LAYER_INFO`'s strings (~line 5443, shown in the click-to-open `#layer-info-popover` via the small "ⓘ" button on each layer card) are unwrapped mixed HTML: bare text runs (containing `MO`/`ME`/`JTBD`/etc.) sitting directly alongside `<strong>`/`<em>` tags as siblings, not nested inside one root `<div>` like every other modal's render output. Those bare text nodes get reported as top-level `addedNodes` with `nodeType === 3` and were silently skipped. Fixed by re-scanning `mut.target` (the container itself) instead of iterating `addedNodes` — catches both wrapped and unwrapped content uniformly, and is simpler code. Verified: Strategy layer info popover now underlines `MO`/`MP`/`MW`/`ME`, Innovation layer's underlines `JTBD`/`ODI`.
- **Removed `AI` from `ACRONYM_GLOSSARY`** per Andrea's explicit call — it no longer gets the underline/popover treatment anywhere (glossary-driven, so this was a one-line removal, no markup changes needed).
- Regenerated `SITE_CONTENT.md` again after these fixes (no content text changed, just keeping the build artifact in sync per project convention).

**State at end of session:** All changes live in `index.html` and `tools/extract-content.js`'s regenerated output (`SITE_CONTENT.md`). Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-14 (cont'd) — Replaced Historic Context with a new "Momentus Offering" section

Andrea asked to replace the Historic Context timeline entirely with a section listing actual Momentus products and which pieces of each serve which stage of the event lifecycle (using this map's own 8 JTBD stages, not a date-sorted history).

- **Research first, implementation second.** Mined the 11+ local PMO strategy PDFs, `Event Lifecycle & Verticals.pptx`, and the WeTrack strategy doc for real product/module names and any stage-language ties. None of the source docs use this map's own 8-stage vocabulary (Define/Locate/Prepare/Confirm/Execute/Monitor/Modify/Conclude) — each uses its own phase language (Intake/Plan/Execute/Report; WeTrack's "Control"; etc.), so most stage assignments are this map's own inference, not literal source statements. Verified the load-bearing quotes myself directly (WeTrack pptx slide 42's "Control should be the main operations tool with Elite/Enterprise focusing on event planning, inventory, and finances," the Enterprise Accounting module ladder) rather than trusting the research sub-agent's quotes at face value, per the project's standing rule.
- **3 decisions confirmed with Andrea before implementing** (see her exact framing for future reference): (1) keep the same 5 area cards (Sales/Planning/Operations/Financial/End-to-End), filter by product instead of category — matches existing UI pattern, less rework; (2) Elite gets exactly one honest, thin entry rather than a fabricated module breakdown, since no source describes Elite's internal modules the way Enterprise/WeTrack's are described; (3) inferred stage mappings are included but explicitly flagged — Andrea's own words: *"since this is a map meant to simplify and share a clear language it is important to clarify in this section when we are using other terms in each of the products, so for example, if in elite we call operations 'execution' and enterprise we call it 'delivery' you should make sure to make it clear that both terms must be inferred to be synonyms to 'operations'."* This is why every entry has both an `inferred` flag (badge) and a `sourceTerm` field (dedicated translation note) — see `ARCHITECTURE.md`.
- **Implementation:** replaced `HISTORIC_CONTEXT`/`HC_CARD_META`/`HC_CAT_META` and `openHcModal()`/`hcFilter()`/`renderHcModal()` with `MOMENTUS_OFFERING`/`OFFER_CARD_META`/`PRODUCT_META` and `openOfferModal()`/`offerFilter()`/`renderOfferModal()` (`index.html`). Same 5 static area cards, same modal shell, same icon-badge pattern — swapped the clock badge for a small package/box icon to signal "product" instead of "history." New `--prod-enterprise/-elite/-wetrack/-platform` CSS tokens replace the removed `--hist-industry/-customer/-product` ones (reused the exact same indigo hex for Enterprise that "Product History" used, freed up by the removal). Modal groups entries by stage (with an "Infrastructure (cross-cutting)" bucket for entries with an empty `stages` array, e.g. Momentus Connect) instead of sorting by date.
- **21 entries curated** across Enterprise (12, the richest-sourced product — CRM, Accounting Suite tiers, Inventory & Assets, BEO, GRC, AskMomentus, etc.), WeTrack (5 — Planning, Control, Hub, Sustainability, Project Management), Elite (1, deliberately thin), Platform (3 — Analytics/OneView, Connect, Shared Services Framework, the latter two cross-cutting). Confirmed "Booking Manager" and "Event Coordinator" are role/persona research docs, not products — excluded from the map entirely to avoid misrepresenting the source material.
- Updated `tools/extract-content.js`: swapped the vm-extraction names list and rewrote the `SITE_CONTENT.md` render section to group by area card then by stage (mirrors `renderOfferModal()`'s logic), with the same inferred/source-term annotations inline. Regenerated successfully.
- Updated `ARCHITECTURE.md`'s content model with the new schema, replacing the Historic Context documentation.
- Verified live in-browser (dark + light themes): all 5 cards, product filter chips, stage grouping, the "Infrastructure (cross-cutting)" bucket, inferred-mapping badges, and the source-term translation note (WeTrack's "Control," Elite's "Scheduling & Coordination") all render correctly; confirmed the acronym hover-underline feature (`GL`, `CRM`, etc.) picks up the new modal content automatically via the existing `MutationObserver`, no extra wiring needed. No console errors. One redundancy caught during verification and fixed: the WeTrack Control entry's `desc` originally re-explained the "Control" translation in prose *in addition to* the dedicated `sourceTerm` note — trimmed the prose since the UI already surfaces it once, cleanly.

**State at end of session:** All changes live in `index.html`, `tools/extract-content.js`, `ARCHITECTURE.md`, and this file. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git. **Known gaps, not yet acted on:** Elite has no sourced module-level detail (flagged inline on the map itself, not hidden); Modify is the one JTBD stage with zero mapped offerings across every product (an honest gap, not a bug); Andrea said she'll supply more information on this section as it develops.

### 2026-07-14 (cont'd) — Icon rework, then corrected a misunderstanding about "stages" + new exact product catalog

**Icon iteration on the 5 area cards:** Andrea liked the earlier clock→box badge swap but asked to invert it — box as the big 48px icon, the persona's object icon (folder/book/square/circle/calendar) as a small badge centered on the box like a label. First pass reused the old isometric 3D box path scaled down as the badge; Andrea then gave a reference image of a simpler front-view open box (rectangle + peaked flaps) and asked to use that instead — implemented, but the flap peak read as a house roof ("looking pointy and not like a box"). Fixed by removing the shared peak entirely: two separate flap lines that don't meet (`M3 9 9 4` / `M21 9 15 4`), leaving a gap at top — reads as two open flaps, not a roof. Verified all 5 by cloning each card's inline SVG into a large temporary fixed-position overlay via `preview_eval` (screenshot tool doesn't crop, so this was the only way to inspect a 48px icon at readable size), removed after each check. Also added a checkmark inside Operations' square badge and a dollar sign inside Financial's circle badge, both hand-drawn to fit the same badge coordinate space (Operations: simple 3-point polyline; Financial: a scaled-down copy of Feather's dollar-sign icon nested in its own sub-`<g>` transform).

**Bigger correction — "stages" meant areas, not JTBD stages.** Andrea clarified that when she originally asked for "products and the pieces of it that serve each event lifecycle stage," she meant the 5 area cards (Sales/Planning/Operations/Financial/End-to-End) — not this site's 8 JTBD stages (Define/Locate/.../Conclude), which is how the section had been built. This was a real misunderstanding from the original request, not a small tweak. Also: Andrea added `Momentus_Product_Catalog_Exact.docx` to the project root as the authoritative reference for Momentus's actual commercial product portfolio, to replace the earlier PMO-strategy-doc-inferred product list.

- **Read the new doc directly** (`textutil` + raw docx XML check — it's 2 flat paragraphs, no tables, confirmed nothing was lost in the plain-text conversion). It names **7 products**, a meaningfully different taxonomy than the 4 (Enterprise/Elite/WeTrack/Platform) used before: **Enterprise** and **Elite** (both cover all 5 lifecycle areas — the two systems of record), **WeTrack** (Planning/Operations/Financials only — production & live-event project management: project mgmt, production planning, crew coordination, equipment logistics, budget tracking, task management, collaboration), **Risk & Incident Management** (Operations only — a *separate* product line from WeTrack in Momentus's own go-to-market catalog, even though the actual capabilities — Risk Register, Incidents, Response Plans — live in WeTrack's product strategy doc; kept both facts visible rather than picking one), **Exhibition Solutions** (Sales/Planning/Operations/Financials — booth/sponsorship sales, exhibitor management/ordering, show operations), **Sales AI** (Sales only), and a **Platform** layer (universal — APIs, Reporting & Analytics, Shared Customer/Event Data Model).
- **Rebuilt `MOMENTUS_OFFERING` from scratch** (`index.html`) using the exact catalog as the primary source for product/area coverage, keeping previously-verified PMO-doc module detail for Enterprise/WeTrack where it didn't conflict (e.g. Enterprise Accounting Suite tiers, BEO, GRC are still there). Dropped the `stages` (JTBD array) and `sourceTerm` fields entirely — no longer needed now that "stage" means area, which the `area` field already captures, and the new catalog uses the same area vocabulary directly so there's no cross-product terminology translation problem to solve anymore. Kept a lighter `inferred` boolean for the handful of cases where a product's flat capability list had to be split across areas without an explicit per-capability tag (e.g. Exhibition Solutions' Financial entry).
- **Rewrote `renderOfferModal()`**: removed the `STAGES.map(...)` grouping and the "Infrastructure (cross-cutting)" bucket entirely; now groups the current area's filtered entries by `PRODUCT_ORDER` (7 products), one header per product with entries. Removed the now-redundant per-item product-tag chip (the group header already says the product). Added `--prod-risk`/`-exhibition`/`-salesai` CSS tokens (dark + light) alongside the existing enterprise/elite/wetrack/platform ones; reused the freed-up old `--hist-industry` (red) and `--hist-customer` (lime) hexes for risk/exhibition respectively.
- Updated `tools/extract-content.js` to match (group by `PRODUCT_ORDER` instead of `data.STAGES`; added `PRODUCT_META`/`PRODUCT_ORDER` to the vm-extraction names list, which had been missing `PRODUCT_META` even in the original build — caught while wiring this). Regenerated `SITE_CONTENT.md` successfully.
- Updated `ARCHITECTURE.md`'s Momentus Offering section to state the terminology correction explicitly up top (so a future session doesn't repeat the JTBD-stage mistake), document the 7-product taxonomy, and name `Momentus_Product_Catalog_Exact.docx` as the primary source.
- Verified live in-browser: product filter chips now show all 7 (wraps to 2 rows), grouping by product within each area card confirmed via the Operations card (Enterprise → WeTrack → Risk & Incident, in `PRODUCT_ORDER`), filtering to a single product isolates correctly (checked "Risk & Incident"). No console errors.

**State at end of session:** All changes live in `index.html`, `tools/extract-content.js`, `ARCHITECTURE.md`, and this file. `SITE_CONTENT.md` regenerated. `Momentus_Product_Catalog_Exact.docx` added to the project root as a primary reference doc. Not yet uploaded to the Claude Project. Nothing committed to git.

**Follow-up same session — two small fixes, one of them a real latent bug caught along the way:**
- **Filter chips now only show products that actually have entries for the current area card**, instead of always listing all 7 regardless of relevance. `renderOfferModal()` now computes `availableProducts` from the area-filtered set (before the product filter is applied) and only renders chips for those.
- **While verifying that, found the `area:'universal'` convention was silently broken.** `ARCHITECTURE.md` (and the original 2026-07-06 Historic Context session log it was copied from) documented "universal entries surface on every card," but the actual filter — `meta.areas.includes(e.area)` — only ever matched an entry's exact area; `'universal'` never satisfied `['sales'].includes('universal')`. So Elite, AskMomentus, and all 3 Platform entries were only ever reachable via the End-to-End card (`areas:null`, which bypasses the filter and shows literally everything), never via Sales/Planning/Operations/Financial individually — despite Elite being confirmed to cover all 5 lifecycle areas. This bug predates today's rebuild entirely; it's been latent since the original Historic Context implementation, just never surfaced because nobody had reason to check what showed up on, say, the Sales card specifically. Andrea confirmed universal-on-every-card was the intended behavior and asked for the fix. Changed the filter in both `index.html`'s `renderOfferModal()` and `tools/extract-content.js` to `!meta.areas || e.area === 'universal' || meta.areas.includes(e.area)`. Regenerated `SITE_CONTENT.md` and verified live: all 5 cards now correctly include Elite + Platform (chip list per card: Sales → Enterprise/Elite/Exhibition/Sales AI/Platform; Planning/Financial → Enterprise/Elite/WeTrack/Exhibition/Platform; Operations → + Risk & Incident; End-to-End → all 7). No console errors.

**Follow-up same session — replaced the filter chips with a plain expandable list.** Andrea asked to remove the chips entirely and make each product row expand in place to reveal its capabilities. Removed `offCurrentProduct` state and `offerFilter()` entirely — `renderOfferModal()` now always lists every product relevant to the current area as a collapsed row (chevron + colored product name + item count), and a new `toggleOfferGroup(productId)` function toggles that one product's `.off-group-body` open/closed by id, independent of the others (multiple can be open at once — no accordion-style mutual exclusion, since nothing asked for that). Chevron rotates 90° via a `.open` class on click. Removed the now-dead `.off-filter-row`/`.off-chip*` CSS, added `.off-group`/`.off-group-head`/`.off-group-chevron`/`.off-group-label`/`.off-group-count`/`.off-group-body`. `tools/extract-content.js` untouched — the markdown export already lists everything flat under per-product headers with no interactivity to model, so the chip removal doesn't affect it. Verified live in both themes (Operations card expand/collapse, Sales card content readable in light mode, acronym-hover still intact on expanded content) — no console errors.

**Follow-up same session — removed color-coding from product names.** Andrea asked to stop color-coding the `.off-group-label` text. Dropped the `style="color:${prod.color}"` inline style and the now-unused `const prod = PRODUCT_META[p]` lookup line in `renderOfferModal()` (kept `PRODUCT_META[p].label` inline instead), added an explicit `color: var(--text)` to the `.off-group-label` CSS rule so it doesn't silently inherit something unexpected. `PRODUCT_META`'s `color`/`bg` fields and the `--prod-*` CSS tokens are now unused by this section's rendering — left them in place (harmless, cheap to keep in case color comes back for something else) rather than deleting, since that wasn't asked. Verified live — no console errors.

### 2026-07-14 (cont'd) — Major rebuild: real Support Center Knowledge Base becomes the primary source

Andrea added a `Support Center Knowledge Base/` folder: 121 real PDFs exported from the actual Momentus Support Center, Elite Support Center, WeTrack Support Center, and Momentus Connect help centers — genuine customer-facing product documentation, a different tier of authority than anything used on this map before (PMO internal strategy decks, or Andrea's own commercial catalog doc). She asked me to find and read it, then (after I reported back a categorized summary) explicitly said yes to using it to substantially rebuild `MOMENTUS_OFFERING`.

- **Extraction:** used PyMuPDF (`fitz`) to bulk-extract plain text from all 121 PDFs into `/tmp/sckb_extract/*.txt` (~475K characters total, ~3,900 chars/doc average) — scratch files, not committed to the project. Parsed each file's breadcrumb (`Community <path> Follow` / `Momentus Support Center > Category > Subcategory`) to recover the real information architecture rather than relying on filenames alone.
- **Key structural discovery:** Enterprise's docs are already organized as one real module per file (Accounts & Contacts, Campaigns, Contracts, etc. are literally separate Support Center categories) — near 1:1 with the old file list. Elite's docs are much coarser at the top level — e.g. 9 separate PDFs (Create a Custom Operations View, Download Event Orders, Event Dashboard, Jobs Fulfillment, Jobs Management, Operations Daily View, Operations Hub Overview, Operations in Elite, plus "Operations" itself) all collapse into ONE real category, `Momentus Elite > Operations`. Consolidated by real breadcrumb category rather than by file, which is why Elite went from 47 files to 25 module entries (Enterprise stayed closer to 1:1 at 41 entries from ~39 files).
- **Delegated the first-pass draft to a general-purpose agent** (read all 121 files, draft `{product, module, area, inferred, desc, source}` entries, consolidate by real category, and explicitly flag rather than silently resolve any tension with the existing data model) — consistent with this project's standing rule (see 2026-07-06 session) to not trust sub-agent content judgments without verification. Spot-checked ~6 of its more consequential/surprising claims directly against the raw extracted text myself before accepting anything (Ask Mo's real sub-parts, Momentus Analytics' real tiers, WeTrack's Risk breadcrumb, Enterprise's Exhibition Management breadcrumb, Elite's Exhibitor Ordering breadcrumb, Operations Hub's real description) — all confirmed accurate.
- **Real `sourceUrl`s added for the first time.** Every extracted PDF's footer contains its real live Support Center URL (`supportcenter.ungerboeck.com`, `elitesupportcenter.ungerboeck.com`, `wetracksupportcenter.ungerboeck.com`, `connectsupportcenter.ungerboeck.com`) — scripted a lookup (first `https://` match per file) and wired all 85 new entries to their real article link, so `off-item-source` now renders live clickable links instead of always falling back to a plain "Source: ..." string.
- **3 structural discrepancies surfaced, not silently resolved** (see `ARCHITECTURE.md` for the canonical writeup): (1) "Risk & Incident Management" is a real WeTrack module (`WeTrack > Risk`, `WeTrack > Control > Incident Management`) in the live docs, not the separate product the commercial catalog claims — kept both the real WeTrack-tagged entries and the original `risk`-tagged entry, each cross-referencing the other. (2) Same pattern for "Exhibition Solutions" — Enterprise has its own Exhibition Management/Floor Plan modules, Elite has its own Exhibitor Ordering module; kept the `exhibition` bucket but noted the real docs show it built into the core products instead. (3) Zero support-doc evidence for "Sales AI" anywhere in the 121 articles — the only AI product is Ask Mo, which is support/data Q&A, not sales-specific; kept the existing entry but flagged it as thinly sourced (absence of evidence isn't evidence of absence — it may just be newer/lower-adoption).
- **Corrections made to prior entries** based on the real docs: "AskMomentus" → renamed **Ask Mo**, moved from `enterprise` to `platform` (it's a Momentus Connect capability with Support + Data parts, not Enterprise-specific). "Momentus Payments (Accounting integration)" → renamed **Momentus Payments Platform**, moved to `platform`. Momentus Analytics' real tiers are **Observe / Empower / Benchmarking** (previous entry said "Empower Pro," which isn't real). Room Diagrams are confirmed still live today, not retired — EventDraw is an additional tab alongside it, correcting an earlier PMO-doc-sourced claim that Room Diagram was being phased out. Elite has no native GL/accounting module — its only documented bookkeeping path is a third-party **Xero + Elite Integration**, a genuinely new and notable finding about Elite's actual financial-area capability.
- **Net change:** `MOMENTUS_OFFERING` went from 24 entries to 92 (enterprise 41, elite 25, wetrack 12, platform 8, risk 1, exhibition 4, salesai 1). Enterprise and Elite in particular went from thin/generic to fully real, module-by-module coverage — Elite especially, which previously had exactly one deliberately-thin placeholder entry (`area:'universal'`, "no sourced module breakdown exists yet") that's now fully superseded and removed.
- Kept 2 forward-looking Enterprise roadmap entries from the original PMO strategy docs (Operations Change Log, native BEO builder) since they're unreleased-as-of-now features that wouldn't be expected to appear in live customer support docs yet — labeled accordingly rather than presented as live.
- Rewrote the data-comment header in `index.html` and the "Strategy Layer — Momentus Offering" section of `ARCHITECTURE.md` to name the Support Center Knowledge Base as the new primary source and document all 3 structural discrepancies explicitly, so a future session doesn't quietly "fix" the risk/exhibition/salesai entries into matching the real docs without knowing why they were kept separate.
- Regenerated `SITE_CONTENT.md` (298.5 KB, up from 254 KB) — extraction ran clean. Verified live in-browser in both themes: Sales card now shows Enterprise (11)/Elite (13)/Exhibition (1)/Sales AI (1)/Platform (7); Operations card's Risk & Incident group renders its tension note correctly; clicking a real source link opens the actual Support Center article; total item count across all 7 groups on the End-to-End card sums to 92; acronym hover-underline still works inside the new content (spot-checked "BEO"). No console errors.

**State at end of session:** All changes live in `index.html`, `ARCHITECTURE.md`, and this file. `SITE_CONTENT.md` regenerated. `Support Center Knowledge Base/` (121 PDFs) added to the project root as a primary reference source. Not yet uploaded to the Claude Project. Nothing committed to git. Scratch extraction files (`/tmp/sckb_extract/`, `/tmp/full_offering_block.js`, etc.) are outside the project folder and were not committed or left behind in the project itself.

### 2026-07-15 — Removed all Momentus_Product_Catalog_Exact.docx content

Andrea asked to remove everything sourced from `Momentus_Product_Catalog_Exact.docx`, the internal commercial catalog doc used before the real Support Center sweep. That doc backed exactly 8 `MOMENTUS_OFFERING` entries: WeTrack's "Budget Tracking," the entire `risk` bucket ("Risk & Incident Management," 1 entry), the entire `exhibition` bucket (4 entries: Booth & Sponsorship Sales, Exhibitor Management, Exhibitor Ordering & Show Operations, Exhibitor Billing), the entire `salesai` bucket ("Sales AI," 1 entry), and Platform's "Shared Customer/Event Data Model."

- Removed all 8 entries from `MOMENTUS_OFFERING` (92 → 84 total: enterprise 41, elite 25, wetrack 11, platform 7).
- Since `risk`, `exhibition`, and `salesai` had zero entries left, removed them entirely from `PRODUCT_ORDER` and `PRODUCT_META` — the product filter/grouping is now just `['enterprise', 'elite', 'wetrack', 'platform']`. This also retires the "known discrepancy" framing from the previous session (Risk/Incident-is-really-WeTrack, Exhibition-is-really-Enterprise/Elite, Sales-AI-has-no-evidence) — those tensions only existed because the catalog doc's framing was being kept alongside the real one. With the catalog-doc entries gone, there's nothing left to reconcile.
- Cleaned up the now-orphaned `--prod-risk`/`--prod-exhibition`/`--prod-salesai` CSS custom properties (dark + light theme) — no longer referenced by any `PRODUCT_META` entry.
- Rewrote the `MOMENTUS_OFFERING` data-comment header in `index.html` and the "Strategy Layer — Momentus Offering" section of `ARCHITECTURE.md` to reflect the 4-product model and explicitly note that `Momentus_Product_Catalog_Exact.docx` is no longer cited anywhere — including guidance for a future session: if exhibition/risk/sales-AI capability needs representing again, model it as real modules under the 4 existing products (per what the Support Center docs actually show), not as a new product bucket backed by the internal catalog doc.
- Regenerated `SITE_CONTENT.md` (290.6 KB). Verified live in-browser: all 5 area cards now show exactly 4 product groups (Enterprise/Elite/WeTrack/Platform) summing to 84 items total on the End-to-End card, no console errors.

**State at end of session:** All changes live in `index.html`, `ARCHITECTURE.md`, and this file. `SITE_CONTENT.md` regenerated. Nothing committed to git.

### 2026-07-15 (cont'd) — Incorporated Carissa's front-of-house/back-of-house feedback into the Operations Human Layer

Andrea relayed stakeholder-expert feedback: when asked about the "Operations Supervisor" persona, Carissa clarified that operations actually splits into front-of-house (ticketing, customer-facing staff) and back-of-house (AV, catering, security) department heads, who may or may not share a director above them depending on the org — "are they front of house or are they back of house?" is the key question for reading any operations stakeholder.

- **Investigated before proposing anything:** read `PERSONAS_DETAIL.owen`, `SECONDARY_PERSONAS.owen` (6 entries), and the relevant `VERTICAL_ATTRS.owen` variants to see how Operations was currently modeled. Found Owen's existing `structural[1]` bullet already claimed unified authority over both front- and back-of-house — exactly the framing Carissa was correcting — and that none of the 6 existing secondary personas were explicitly classified by side.
- **Proposed an approach and asked for confirmation** (per this project's standing rule on high-blast-radius primary-persona changes) rather than just implementing. Andrea's answers: (1) make Owen the Operations Director who manages FOH/BOH department heads, and classify each secondary persona as FOH or BOH; (2) put the FOH/BOH framing in the unified `PERSONAS_DETAIL.owen` attributes, not a vertical-specific variant.
- **Changed Owen's title**: `PERSONAS_DETAIL.owen.role` "Jordan Olsen · Operations Supervisor" → **"Jordan Olsen · Operations Director"**. Swept every live-rendered reference for consistency: `nsMeta.owen.pRole`, `PERSONAS_J`'s owen `role` field, the Stages View header (`.st-card-fw`), and the persona-strip label (`.prole`) — 6 call sites total. Deliberately left `VERTICAL_ATTRS.owen`'s per-vertical title variants untouched (Convention Centers' "Owen Olsen · Operations Supervisor," Corporate's "Oliver Otto · Operations Lead," etc.) per Andrea's "not vertical specific" answer — these were already varied per vertical before this change, so there was no prior consistency to preserve there anyway. Also left the known-dead `.zi-grid` legacy markup untouched, per its existing "don't treat as current" status in `ARCHITECTURE.md`.
- **Rewrote Owen's `structural[1]` bullet** from claiming direct unified authority over both sides to: sits above two department heads (FOH: ticketing/customer-facing staff; BOH: AV/catering/security) rather than delegating directly to crews, explicitly noting those two heads may report to separate leadership rather than a shared director depending on the org, and naming "front of house or back of house" as the first question to ask about any operations stakeholder.
- **Classified all 6 `SECONDARY_PERSONAS.owen` entries** by prepending an explicit tag to each `relationship` field: Orlando Olive (ground crew), Anna Adams (AV Technician), Carrie Carol (Control Room), and Freddie Ford (Meeting Services Crew) → **"Back-of-house:"**; Hank Henry (Risk) and Paige Pepper (Public Events Manager) → **"Spans both front-of-house and back-of-house:"** (both are explicitly cross-functional per their own existing structural text, so didn't force them into one bucket). Also updated the 2 in-text "Operations Supervisor" references inside Orlando Olive's own `fixed`/`structural` bullets to "Operations Director" for consistency, since those specifically name Owen.
- **Surfaced an honest gap rather than fabricating content:** all 6 existing secondary personas turned out to be back-of-house or cross-functional — there is currently no front-of-house-only secondary persona (ticketing, box office, guest services) anywhere under Owen. Documented this explicitly in `ARCHITECTURE.md` as a known gap, flagged to Andrea, not invented.
- Regenerated `SITE_CONTENT.md` (291.0 KB). Verified live in-browser (both themes): Owen's persona modal shows "Operations Director" and the rewritten structural bullet (with "AV" correctly picking up the acronym hover-underline automatically); Anna Adams' and Hank Henry's secondary persona modals show their new FOH/BOH tags clearly in both dark and light theme. No console errors.

**State at end of session:** All changes live in `index.html`, `ARCHITECTURE.md`, and this file. `SITE_CONTENT.md` regenerated. Nothing committed to git.

### 2026-07-16 — Two Operations stakeholder interviews synthesized; sitewide name removal

Andrea shared two Dovetail interview recordings (an Enterprise-focused discovery interview and a WeTrack/FIFA-context validation interview) for the Operations Product Strategy Discovery project and asked for insights to enrich the Operations Human/Innovation/Strategy layers. Extracted ~15 candidate changes, presented individually per her explicit ask ("confirm each change so I can accept, dismiss, or redirect it") — most were approved with revisions.

**Implemented this session (Owen = Operations Director throughout):**
- **Innovation Layer, Define stage:** 2 new desired outcomes — plan-to-work-order handoff effectiveness (kept product-agnostic per Andrea's correction to my first, too-product-specific draft), and missing/conflicting information in that same handoff causing rework.
- **Innovation Layer, Prepare stage:** new desired outcome on deriving a work order's start time from how long that type of work actually takes — reframed per Andrea's correction: not about generic setup/teardown calendar windows, but per-task-type duration forecasting so crew know when to *begin*, not just their deadline.
- **Innovation Layer, Modify stage:** research-opportunity addition — surfacing the *calculated downstream effect* of a change (e.g. +50 attendees → +50 units across specific catering line items/dates), not just a bare change notification.
- **Innovation Layer, Conclude stage:** research-opportunity addition — operations already produces manual end-of-shift/end-of-day summaries; that's a more realistic dashboard data-capture surface than new data entry.
- **Human Layer, `PERSONAS_DETAIL.owen.structural`:** added a standalone front-of-house/back-of-house definer bullet (with examples) positioned before the existing dual-scope note, since Andrea wanted the terms explained before being used elsewhere; added a bullet stating back-of-house departments carry dual scope (event-specific + ongoing venue-maintenance duties); added a bullet establishing the Operations Director as the platform's actual buyer/decision-maker, with crew/staff (secondary personas) mattering to this map only for what the Director depends on them for — task execution and data entry. This last point came from Andrea correcting my initial framing, which had incorrectly suggested "ops isn't a buyer" applied to the primary persona itself rather than to the secondary crew personas underneath it.
- Regenerated `SITE_CONTENT.md` after each batch; syntax-verified via `node extract-content.js`.

**Deferred, not yet implemented (Andrea's call pending):**
- A full reframe of the Operations job from "Execute Event Work" to **"Orchestrate Event Work"** — Andrea's correction that the primary persona is the Director, not the crew physically executing — plus a full redraft of all 5 Momentus Work North Star input metrics around the Director (Efficiency → literal time-to-value for the Director's team, not task cycle time; Effectiveness → whole-event orchestration success, not single-task completion; Engagement → per-department event coverage, a "breadth" metric per the Amplitude North Star Playbook's own Breadth/Depth framing, replacing a per-event average the interview subject explicitly called a bad metric). Drafted and presented; awaiting Andrea's confirmation before touching `index.html` given the blast radius (mirrors the `e3`→`me` rename in scope).
- The Locate-stage turnover-window research-opportunity note (validating an existing desired outcome against the interview) — Andrea asked to discuss further before drafting anything; not yet resolved.
- A bullet list of every topic the interview subject raised specifically about the Effectiveness metric was delivered directly in conversation (not written to any file) — Andrea's next move on the MW Effectiveness definition depends on both that list and the Orchestrate reframe above, so intentionally left unresolved together.

**Sitewide name removal (Andrea's explicit, standing instruction going forward):** while drafting the above, Andrea caught me referring to a persona by "Emilie" — actually just the internal code id, not a real display name (a mistake this file already warned against, see Core IDs in `ARCHITECTURE.md`). She asked to remove every personal name from the map entirely and refer to personas by role only, from now on, in both the file content and conversation.
- **Scoped first, per this project's standing rule against un-scoped invasive changes.** Asked Andrea two clarifying questions: (1) whether the historical CLAUDE.md Session Log should also be scrubbed — she said no, it's a historical record and keeps names; (2) how to handle now-nameless quote attribution and secondary-persona identity — she chose role-only (e.g. "— Jordan Olsen" → "— The Operations Director"; secondary personas identified by role + vertical, which was already unique per entry with no name needed).
- **Extracted every name-bearing field programmatically** (a small `vm`-based script reusing `extract-content.js`'s sandbox technique) rather than relying on grep alone, given this project's repeated prior experience of sub-agent/manual sweeps under-counting (MWO/BEO, ARR misses in earlier sessions) — found all 5 `PERSONAS_DETAIL.role`/`quoteSrc` pairs, all 17 `VERTICAL_ATTRS[..].persona` fields, all 18 `SECONDARY_PERSONAS[..].name` fields, and all 5 `PERSONAS_J[..].name` fields.
- **Then grepped every individual first name** (not just full name pairs) across the whole file to catch prose cross-references missed by the structured-field extraction — found ~18 parenthetical name-drops inside `fixed`/`dynamic`/`structural` bullets (e.g. "Bookings Manager (Bettie)," "risk (Hank)," two never-modeled inline names "Fred"/"Ella" inside Anna Adams' bullet, "Carole" inside a Sam Evans vertical bullet) plus several Rationale-prose mentions in the North Star section ("Jordan is the last mile," "Sam documents...") and 4 places (`.pname` in the North Star persona-strip, `.st-card-name` static placeholders, 2 hardcoded aria-labels, `LAYER_INFO.human`) using either a real name or — same mistake as the "Emilie" catch — an internal id where a name/role was expected.
- **Deleted the entire dead `.zi-grid` legacy block** (160 lines, `display:none`, superseded per this file's own 2026-07-01 session log and already flagged as "a reasonable candidate for cleanup") rather than scrubbing names inside otherwise-unrendered markup — removes its duplicate name content and the flagged debt in one move.
- **Fixed 3 destructuring call sites** (`openPersonaModal`, `renderStagePage`, and the persona-card builder) that did `const [personaName, jobTitle] = d.role.split(' · ')` — now that `role` is title-only with no separator, simplified each to `const jobTitle = d.role`. Also fixed 2 render lines showing `${p.name} · ${p.role}` (JTBD headers) down to just `${p.role}`, and the secondary-persona modal/tile's `${sp.name}` references down to `${sp.role}`/dropped entirely where redundant with the archetype already shown.
- **Fixed `tools/extract-content.js` too** — it had its own independent logic reading `d.role.split(' · ')`, `d.quoteSrc`, `s.name`, and `p_.name` to build its Markdown headers; without the fix these all rendered as literal `undefined` in the regenerated `SITE_CONTENT.md` (caught by reviewing the regenerated file, not assumed). Also renamed the North Star `.persona-strip` scraper's `{name, role}` object to `{role, archetype}` to match what those DOM nodes actually contain now.
- **Updated `ARCHITECTURE.md`** (Core IDs, Human Layer section, Owen's core-identity note) to describe the no-name schema going forward and explicitly instruct future sessions not to reintroduce a `name`/`quoteSrc` field. **Updated `CLAUDE.md`'s own Personas list** (this file, above) the same way. Left the Session Log (this section and everything above it) untouched per Andrea's explicit answer.
- **Left one thing untouched, flagged to Andrea:** an internal-only CSS comment (`--sharon`/`--owen` color-token block, ~line 27) uses "Sam"/"Jordan" as developer shorthand for two personas in a historical color-migration note. It's dev-facing only, never rendered, and already somewhat garbled (a pre-existing typo duplicates "Sam" for two different colors) — left as-is since it's not "the map's content," but flagged in case Andrea wants it gone too.
- Regenerated `SITE_CONTENT.md` (292.4 KB). Verified live in-browser: Operations Director persona modal, North Star persona-strip, and a secondary-persona tile/modal all render role-only with no console errors; acronym hover-underline still works inside the untouched parts of the affected bullets.

**State at end of session:** All changes live in `index.html`, `tools/extract-content.js`, `ARCHITECTURE.md`, and this file. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git. Open items for next session: the Orchestrate Event Work reframe + 5 North Star metrics (drafted, awaiting confirmation), the Locate-stage turnover-window discussion, and the Effectiveness metric definition (blocked on both of the above).

### 2026-07-16 (cont'd) — Implemented the Orchestrate Event Work reframe + all 5 MW input metrics

Andrea resolved the two items left open above: (1) the Locate-stage turnover-window discussion needs no change — she confirmed the existing desired outcome #10 in that stage already covers it; (2) implement the Orchestrate/metrics reframe as drafted.

- **`PERSONAS_J`'s owen entry:** `mainAction` "Execute Event Work" → **"Orchestrate Event Work"**; `job` reworded from "Execute all work orders..." to "Direct all work orders required to set up, run, and tear down an event — deploying crew and resources efficiently across multiple simultaneous events through the department heads who report up"; `jtbd.soCan` reworded from "run a flawless event" to "my team runs a flawless event," keeping the Director as the one directing rather than personally executing. Confirmed via grep this is the single source for `mainAction` — every other render site (`p.mainAction`) reads it live, so no other call site needed touching.
- **All 5 `#ns-owen` Key Input Metrics rewritten around the Director**, replacing crew/task-level framing:
  - Adoption: scoped to "Operations Directors or department heads" (was "operations supervisors").
  - Engagement: replaced the per-event average (which the interview validation explicitly called a bad metric) with events-with-a-work-order broken out **by operations department** — a Breadth-style metric per the Amplitude North Star Playbook's own Breadth/Depth framing for engagement, reusing the Event Work object's existing "Department Owner" attribute (Facilities/F&B/AV/Security) rather than introducing new schema.
  - Retention: explicitly scoped to the Director/department-head tier, with a stated reason (crew are frequently no-login and wouldn't be a meaningful signal) — resolves the ambiguity flagged earlier this session.
  - Efficiency: reframed from task-assignment-to-completion cycle time to literal time-to-value — how long until the Director's team is running full orchestration inside Momentus without configuration blockers or outside-tool fallback.
  - Effectiveness: reframed from single work-order completion rate to whether the *whole event's* operations were successfully orchestrated end-to-end (every department's work order created, tracked, closed, no unresolved incident or manual reconciliation) — explicitly no longer measured by any single task checkbox.
- **Caught and fixed a spillover inconsistency while implementing, not pre-cleared with Andrea:** MW's Rationale prose ("The Operations Director's execution currently happens on printed sheets, radio, and email follow-up") directly contradicted the reframe it sits right below — the Director doesn't personally work from paper, the crew does. Reworded to "Execution on the ground currently happens on printed sheets, radio, and email follow-up, leaving the Operations Director to coordinate and reconstruct status manually." Flagged to Andrea rather than assumed acceptable, since it's outside the literal scope of what she asked for.
- Regenerated `SITE_CONTENT.md` (293.3 KB). Verified live in-browser: JTBD grid label reads "Orchestrate Event Work," all 5 `.ic-name` values read back correctly via direct DOM query, no console errors.

**State at end of session:** All changes live in `index.html`. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git. No open items from this interview-synthesis thread remain — everything from the two Dovetail interviews has been either implemented or explicitly resolved as "no change needed."

### 2026-07-16 (cont'd) — Corrected "work order creation" language sitewide: work orders are received, not created, by operations

Andrea caught a semantic error running through the Operations content: work orders aren't created by operations users. Planners create service orders (Planning stage); the system automatically translates those into department-scoped work orders and delivers/hands them off to operations. Operations receives, manages, and acts on work orders — creation itself is automatic and not a meaningful thing to measure or describe as an operations action.

- **Audited every "work order" + creat/build/generat mention in `index.html`** via grep rather than assuming scope from memory — found 18 misattributions across the Strategy and Innovation layers, all now fixed. Confirmed several pre-existing mentions were already correctly framed and left untouched: the Semantic Layer's "Generated From" / "Work Order # — system-generated identifier" attributes, the Enterprise "Work Orders" offering entry ("generated from service orders"), and Operations Director's Stadiums & Arenas vertical dynamic state (a distinct, already-validated vertical-specific gap — that vertical genuinely lacks automated translation, so "translating event needs into work orders entirely by hand" is a real documented pain point, not the same mischaracterization — deliberately left alone).
- **Strategy Layer (Momentus Work North Star):** the top-level `.star-def`, the `MW` acronym glossary description, and the Effectiveness input metric all said work orders are "created, tracked, and closed" — changed to "received, tracked, and closed." Adoption's metric was rescoped from "created or updated" to "received and updated," with a line added explaining why: work orders are auto-generated from the service order, so the signal is whether operations manages them, not whether one exists. Engagement went further — the previous per-department "at least one work order created" framing would always read as ~100% given automatic generation, so it's not a meaningful signal at all; redefined as whether operations actively engages with (views, updates, completes) the received work order vs. defaulting to printed sheets or room diagrams. The Rationale line "it cannot be gamed by creating work orders and never updating them" was reworded to "a work order sitting in the system and never being acted on," since operations was never the one creating it in the first place.
- **Innovation Layer (Locate and Prepare stages, plus one Define outcome):** reworded the Locate stage's own step description ("needed to build and assign work orders" → "needed to review and assign the work orders the system generates from it") and 8 outcome statements that said "building a work order," "work order creation," or similar — now framed as receiving/reviewing a system-generated work order and assigning crew to it. Also reworded Define's "decompose a BEO into individual actionable work units" outcome, which implied operations manually building the work order structure from raw BEO data — now "translate an auto-generated work order into individual crew-level task assignments," since the department-level decomposition is already automatic (see the 2026-07-15 interview-synthesis session's Define-stage work on this) and the real remaining manual step is breaking a department-level work order down to specific crew members.
- Regenerated `SITE_CONTENT.md` (293.7 KB). Verified live in-browser: all 5 `#ns-owen` input-metric definitions read back correctly via direct DOM query, no console errors.

**State at end of session:** All changes live in `index.html`. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-22 — North Star dialog recolor, Strategy Layer "Why We Do It"/"The Trade Off" sections, and Leadership offering rename

Andrea asked for a series of visual/content refinements to the Strategy Layer, implemented incrementally and verified live after each step (both the North Star modal and the main Strategy Layer page, dark theme).

- **North Star dialog (`openNsModal`) Key Input Metrics recolored by stage, not by dimension:** removed the five-dimension color coding (Adoption/Engagement/Retention/Efficiency/Effectiveness each previously had their own accent + background tint via `d-adopt`/`nmc-adopt` etc.) and replaced it with every metric card taking the current product area's own stage color (the same `c`/`bg` already used for the modal header and North Star metric card) — e.g. all five cards read green in the Sales dialog, all orange in the Operations dialog. Dimension labels (Adoption, Engagement, etc.) stay as plain text, just no longer individually colored. The shared `.d-adopt`/`.nmc-adopt`-family CSS classes were left untouched since they're still used by the static (non-dialog) per-product-area page section.
- **Fixed inconsistent spacing above the two dialog headers:** "Product Area North Star Metric" had only 8px of space above it (visionHTML's 4px bottom padding + northStarHTML's 4px top padding) while "Key Input Metrics" had 24px. Increased northStarHTML's wrapper top padding from 4px to 20px so both now sit 24px below the block above them.
- **Added two new sections to the main Strategy Layer page**, below Mission/Vision, matching their card styling: **Why We Do It** — a single merged rationale for the general "event and venue management professional" persona, synthesized from the five product-area `why` statements already in `MOMENTUS_VISION` (previously only visible per-product-area inside each North Star dialog); and **The Trade Off** — struck-through "Act for the persona / Decide for the persona / Ask the persona / Wait for the persona," followed by a bolded, non-struck-through "Amplify the persona."
- **Iterated on the styling of both new sections per feedback:** "Amplify the persona" now uses the same font-size/weight/style as the Mission and Vision slogans (`14px`, `700`, italic) instead of a one-off `800`-weight treatment; the Why We Do It / Trade Off labels, "Amplify the persona," and Vision's own highlighted spans + slogan (previously the separate `--vision-blue` token) were all recolored to the North Star's purple (`var(--me)`) for a consistent accent across the whole Strategy Layer page.
- **Replaced the Why We Do It slogan** — "Stop digging. Stop apologizing. Stop firefighting." → "They hold the expertise, we amplify it." (mirrors the Mission/Vision slogans' cadence and ties directly to the Trade Off section's "Amplify the persona" line).
- **Renamed the Leadership card in Current Offering** from "The Momentus Offering" to "Momentus for Leadership," matching the "Momentus for Sales/Planning/Operations/Financial" naming of its sibling cards (updated the card's display name, its aria-label, and `OFFER_CARD_META.me.label`, which also feeds the offer modal title).
- Regenerated `SITE_CONTENT.md` (331.0 KB). Verified live in-browser: North Star dialogs recolor correctly per stage (confirmed Sales green and Operations orange via direct style inspection), both new Strategy Layer sections render with matching spacing/typography, Vision's highlighted text and slogan read purple, and the Leadership Current Offering card reads "Momentus for Leadership." No console errors.

**State at end of session:** All changes live in `index.html`. `SITE_CONTENT.md` regenerated. **Committed and pushed to `origin/main`** (commit `31446d2`) — this commit also picked up several prior sessions' work that had been sitting uncommitted in the working tree since 2026-07-16 (the sitewide persona-name removal, the Orchestrate Event Work reframe, and the work-order-language correction, each already logged above under its own 2026-07-16 entry). Confirmed live on GitHub Pages via `curl` (new content served, `last-modified` matched the push time); Andrea's stale-content report was a browser cache issue, resolved with a hard refresh, not a deploy problem. Not yet re-uploaded to the Claude Project.

### 2026-07-22 (cont'd) — Re-prioritized Owen's Innovation Layer outcomes by source emphasis, to inform an external Operations Persona Discovery Plan

Andrea is drafting an external customer-interview discovery plan (separate doc, in Confluence/a downloaded PDF, not part of this project) and asked to first re-rank the Operations Director's (`owen`) desired outcomes across all 8 JTBD stages by how much each is emphasized across the sources they were drawn from, reorder each stage's `outcomes[]` array in `index.html` to match, and only then use the re-prioritized outcomes to polish that separate plan's hypotheses and interview questions.

- **Sources triangulated (per Andrea's explicit "all of the above" answer):** `Convention-Center-Findings-Innovation-Layer-Proposal.md` (already carries explicit multi-customer corroboration counts per outcome — the strongest quantified signal available); 3 Dovetail "Ops Persona Internal Validation" recordings in the `Operations Product Strategy Discovery` project (Elizabeth/Katie/Carissa — internal SME interviews about the Momentus Work North Star metrics and WeTrack/FIFA strategy, both highlights and full transcripts read); `Operations - Erin.docx` (single supervisor interview); `Stakeholder Interviews Data Dump.docx` (the original 9-stakeholder foundational validation, ~27K lines, read via each interview's "My Notes!!!" + Actions sections); this file's own Session Log (stakeholder-corrected/validated framings — Director-as-buyer, front-of-house/back-of-house, "received not created" work orders).
- **Verification discipline followed** per this project's standing rule (see 2026-07-01/07-06/07-14 entries): a general-purpose agent did the first-pass Dovetail read (highlights file + all 3 full transcripts) and returned verbatim quotes; before using any of it, spot-checked the most load-bearing quotes (the "cascading impact" theme, the FIFA/WeTrack "platformy" framing, the "phony number" effectiveness debate) directly against the saved highlights file myself — all confirmed accurate, none fabricated.
- **Highest-confidence finding, cross-validated across 3+ independent sources (live site's own Modify research opportunity text, Carissa's Dovetail interview — mentioned 6+ times unprompted as her top strategic recommendation, and the original foundational Andrea Díaz stakeholder interview):** the *calculated downstream impact of a change* (e.g. +50 attendees → specific catering line-item/date quantity increases) is the single most emphasized Innovation Layer theme for Operations anywhere in the sources. Moved to #1 in Modify.
- **Other themes that moved to the top of their stage on multi-source strength:** Define — handoff effectiveness/automatic gap-flagging (Carissa + Katie's "biggest gap" framing + the 2026-07-16 interview synthesis that originally added these outcomes); Prepare — per-task-type duration forecasting driving start times (Katie's most repeated, most concrete ask) and the field-legible combined packet (3-customer Convention Center finding + Erin + Katie); Locate — the turnover-window gap (3-customer Convention Center finding) and the diagram re-upload bug (directly confirmed in Erin's interview); Confirm/Execute — the universal "operations is always the last to know" change theme and manual-data-entry elimination (Carissa's other top theme); Monitor — the unified real-time dashboard concept (Carissa's #2 top theme, FIFA-validated) and planned-vs-actual staffing visibility (Elizabeth + the original stakeholder dump's "we always end up bringing an extra 60 chairs" framing); Conclude — work-order-closure blocking finance invoicing (now cross-validated: Sydney ICC + a Carissa-relayed finance escalation story) and end-of-shift/day manual summaries as the realistic dashboard data-capture surface (Carissa + Erin).
- **Implementation:** reordered all 8 `MAP.owen[stageId].outcomes` arrays in `index.html` in place (no outcome text changed, no additions/removals — pure reordering). Regenerated `SITE_CONTENT.md` (327.3 KB) via `tools/extract-content.js`, which ran clean, confirming no syntax regressions. Verified live in-browser: opened the Innovation Layer's Operations job-timeline modal, clicked into the Modify tab, and confirmed both via DOM read and a screenshot that outcome 01 now reads "Maximize the accuracy of the calculated downstream impact automatically surfaced when a change occurs."
- This ranking was then used to polish the hypotheses and interview questions in Andrea's separate Operations Persona Discovery Plan document (not part of this project's files).

**State at end of session:** All changes live in `index.html`. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-23 — Enriched Operations Director content from a FigJam board ("Life of Ops")

Andrea's PM shared a FigJam board (`Event Ops Lifecycle Strategy`) — a "Life of Ops" flow from booking through post-event, an Ops persona table, and a department-adoption-likelihood note. Cross-referenced every element against the existing Operations Director content before drafting anything, per this project's standing "confirm before structural changes" rule and its "don't trust unverified content" discipline — most of the flow (booking→planning handoff, staffing, BEO, jobs/incidents, reporting) already matched existing JTBD content; only genuinely new material was added.

- **Two structural decisions confirmed with Andrea before implementing:** (1) the FigJam's "Ops Head" role (strategic, cross-account, sets policy, sits above Operations Director) doesn't fit the existing Venue Director secondary-persona pattern — those are all vertical-specific, full P&L "buyer" roles with sales+planning+ops+financials scope, while Ops Head is Operations-only and vertical-agnostic. Andrea initially suggested placing it under Venue Director/leadership; after flagging the scope mismatch, she agreed to place it as a secondary persona under Operations Director instead. (2) The FigJam's Department Heads (Risk/AV/Setup-Inventory/Housekeeping/Security) and End Users (contractors/vendors) read as vertical-agnostic core Operations concepts, unlike the existing vertical-specific secondary personas (AV Technician–Corporate, Operations Crew–Stadiums & Arenas, etc.) — Andrea chose to generalize these as new cross-vertical baseline personas rather than only enriching the vertical-specific ones.
- **3 new `SECONDARY_PERSONAS.owen` entries added** — the map's first generic, cross-vertical secondary personas (`vertical: 'All Verticals'`, no existing precedent for this before today): **Operations Head** ("The Policy Architect" — strategic policy-setting and cross-account benchmarking above Operations Director), **Department Head** ("The Domain Owner" — generic functional-domain lead, complementing the existing vertical-specific department-level personas), **Contractor / Vendor Staff** ("The Field Hands" — generic front-line task-execution role, complementing Operations Crew–S&A and Meeting Services Crew–CC).
- **9 new desired outcomes added to `MAP.owen`**, appended (not inserted) to each stage's `outcomes[]` so existing `OPPS_ACTIONS` `outcomeIdx` references stay valid: Prepare — load-in/dock/room-turnover scheduling; Confirm — auditability of the pre-event readiness check; Monitor — 3 outcomes covering customer requests/complaints/lost & found (none of these existed anywhere in the map before); Modify — 3 outcomes covering automatic policy-compliance checking, billing-impact visibility, and cross-account policy-adjustment recommendations from repeated change patterns.
- **Checked but deliberately not duplicated:** the FigJam's cross-account predictive-staffing sticky ("rugby games always add 2hrs to closeout") is already covered near-verbatim by existing Conclude outcome #3. The FigJam's own "Outside Event Lifecycle" section (inventory tracking, CMMS) was left alone — it's explicitly out-of-scope per the source material itself, and the existing Operations Director Structural Status already acknowledges this same gap.
- **Not acted on:** the FigJam's "Ops Sub-Departments" note (Catering flagged as highest-change, ahead of AV/Facilities/Risk) reads as build-prioritization input, not persona content, and has only a two-word annotation — not enough sourced material to draft a persona from. Flagged to Andrea as a possible future gap (Catering has no dedicated secondary persona today) rather than acted on.
- All new content follows the map's established rules: no persona names (role/archetype only), product-agnostic outcome/step wording, ODI grammar, and Dynamic-vs-Structural classification per system position vs. experience-over-time.
- Verified: JS syntax-checked (`node -e` parse of both script blocks), `tools/extract-content.js` regenerated `SITE_CONTENT.md` (334.7 KB) clean, and verified live in-browser — the 3 new secondary-persona tiles render under Operations (9 total now, up from 6), the Operations Head modal opens with correct title/archetype/vertical tag/relationship/attributes, and the Modify stage's job-timeline modal shows the 3 new outcomes correctly styled. No console errors.

**State at end of session:** All changes live in `index.html`. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-23 (cont'd) — Re-tagged all secondary personas by org-chart level instead of vertical

Andrea decided, immediately after the FigJam-driven Operations enrichment above, that `SECONDARY_PERSONAS` entries sitewide should no longer be tagged by vertical at all — the tile/modal slot that used to show vertical (e.g. "Stadiums & Arenas") now shows the persona's org-chart level instead: **Chief, VP, Director, Manager, Individual Contributor, External/Contractor, Entry Level**. This is a schema change to the whole Human Layer, not just Operations — touched all 21 existing `SECONDARY_PERSONAS` entries across all 4 personas that have any (`sharon`, `emilie`, `owen`, `me`; `dana` still has none).

- **Field renamed** `vertical` → `orgLevel` on every `SECONDARY_PERSONAS[personaId][i]` entry. `VERTICAL_ATTRS` (primary-persona vertical variants) is a separate, untouched structure — this only affects secondary personas.
- **Classification approach:** matched each entry's existing job title to the new taxonomy literally wherever the title already implied a level (e.g. "Booking *Manager*" → Manager, "Entertainment Booking *Director*" → Director), and used judgment grounded in each entry's `relationship`/`structural` text where the title alone didn't map cleanly: front-line/physical crew roles with no system access or reports (Operations Crew, Meeting Services Crew) → Entry Level; specialist roles that execute independently but don't supervise anyone (AV Technician, Control Room Operator, Program Coordinator, Catering Sales Coordinator) → Individual Contributor; the new Operations Head (sits above Operations Director, sets policy across accounts) → VP; the new Contractor/Vendor Staff → External/Contractor (an exact match to the new taxonomy); Executive Director (Arts & Culture) → Chief, since the entry's own structural text describes "ultimate authority over mission, season strategy, and resource allocation" — functionally the top of the org for that vertical, not a peer to the other Director-level entries under `me`.
- **Code changes:** `sp.vertical` → `sp.orgLevel` in both `buildSecondaryPersonaTiles()` and `openSecondaryPersonaModal()` (no markup/CSS changes — same `.sec-persona-tile-arch`/`.sec-persona-tag` slots, just fed by a different field). `tools/extract-content.js`'s secondary-persona heading line updated to match. `ARCHITECTURE.md`'s `SECONDARY_PERSONAS` schema section and "how to add" note updated, plus a new callout explaining the 2026-07-23 change and the 7-value taxonomy for future contributions.
- Verified: JS syntax-checked, `SITE_CONTENT.md` regenerated (334.6 KB) — spot-checked all 21 headings now read `Role (OrgLevel) · "Archetype"` with no stray `vertical` references left (`grep` confirmed). Verified live in-browser: reloaded, read the Operations secondary-persona tiles' full text via the accessibility tree — all 9 tiles show the correct org level (Operations Crew → Entry Level, Health/Safety & Risk Manager → Manager, Operations Head → VP, Contractor/Vendor Staff → External/Contractor, etc.). No console errors.

**State at end of session:** All changes live in `index.html`, `ARCHITECTURE.md`, `tools/extract-content.js`. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git.

### 2026-07-23 (cont'd) — Researched real venue org charts, wrote a title-agnostic `orgLevel` classification rubric, added 2 findings-backed content pieces

Andrea asked for a matrix (Area × org-level) to check secondary-persona coverage, which surfaced a real question: how do you classify `orgLevel` consistently when job titles vary wildly by venue size and vertical? Ran a deep-research workflow (105 agents, 23 sources fetched, 25 claims adversarially verified) across the map's 5 verticals to ground the answer in real sourcing rather than guessing.

- **Research coverage was honest but uneven** — confirmed findings only survived verification for: (1) IAVM's own competency standard, which states title is *not* a reliable proxy for level industry-wide (same job = "supervisor" at a large venue, "senior manager" at a small one); (2) Convention Center Operations' clear hierarchy (GM ← Director of Operations ← managers ← shift managers ← frontline crew, via two independent real job postings); (3) Higher Ed's tendency to consolidate Planning + Operations-liaison + Financial oversight into one Director role rather than splitting them (University of Houston-Downtown's real job description); (4) Arts & Culture's distinct COO layer below the artistic Director (Ithaka S+R's 86-museum org-chart study: ~half of museums have one, managing ~half of staff — finance/HR/facilities/security/operations) plus a discrete C-suite CFO example (Kennedy Center, via IRS 990). **Sales/Booking (any vertical), the entire Corporate vertical, and the entire Stadiums & Arenas vertical returned zero confirmed claims** — every attempted claim there was refuted in adversarial verification. Flagged to Andrea as a real gap needing stakeholder interviews, not more public-web research (this pass already tried and came up empty, not just thin).
- **Wrote a title-agnostic classification rubric** for `orgLevel`, added to `ARCHITECTURE.md` right after the field's own definition: classify by **scope of authority + reporting relationship + employment relationship**, never by matching the title string. One-line definition + "tell" per level (Chief/VP/Director/Manager/Individual Contributor/Entry Level/External-Contractor). Explicit rule: when a role's literal title conflicts with the rubric, the rubric wins, and the discrepancy should be noted in that persona's own `structural[]` text rather than silently resolved — so the reasoning stays visible to future contributors.
- **2 new content pieces added, grounded in the confirmed findings, per Andrea's go-ahead:**
  1. New secondary persona **Chief Operating Officer** ("The Business Backbone") under `me` (Leadership) — `orgLevel: 'VP'`, sourced from the Ithaka S+R museum-COO finding. Deliberately used as a worked example of the rubric itself: the role's own `structural[]` text explains why a title containing "Chief" still classifies as VP (reports into the map's Chief-level Executive Director, oversees a multi-functional swath rather than holding final venue-wide authority) — the exact scenario the rubric exists to resolve. `dynamic[]` kept intentionally modest/structural rather than inventing specific tool-complaint pain points, since this persona is sourced from an org-chart structure study, not a validated stakeholder interview — flagging that distinction rather than blurring it with the map's interview-sourced personas.
  2. Enriched the existing **Contractor / Vendor Staff** secondary persona (`owen`) with a new `structural[]` bullet from the Convention Center Operations finding: at larger venues, an entire functional domain (most commonly catering/F&B) is sometimes run end-to-end by a single contracted operator (e.g. Aramark) rather than individual contracted staff — a bigger concept than the persona previously captured, and it changes who a Department Head coordinates with for that domain.
- Verified: JS syntax-checked, `SITE_CONTENT.md` regenerated (338.3 KB, both new pieces confirmed via `grep`), verified live in-browser — the Chief Operating Officer modal opens with correct title/archetype/"VP" tag/relationship/all three attribute columns, no console errors.

**State at end of session:** All changes live in `index.html`, `ARCHITECTURE.md`. `SITE_CONTENT.md` regenerated. Not yet uploaded to the Claude Project. Nothing committed to git. **Open gap, not yet acted on:** Sales/Booking, Corporate, and Stadiums & Arenas org structures still have no confirmed public sourcing — would need real stakeholder interviews to fill, consistent with how the map's existing Stadiums & Arenas-heavy Operations content was originally built.
